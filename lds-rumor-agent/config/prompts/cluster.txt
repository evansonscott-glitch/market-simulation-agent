You are grouping speculative claims about The Church of Jesus Christ of Latter-day Saints. Two claims belong to the same cluster if they are making essentially the same prediction, even if worded differently.

For each cluster:
1. Assign a cluster_id (format: "cluster-NNNN")
2. Write a one-sentence cluster summary
3. List the rumor_ids that belong to the cluster
4. Count independent sources (same author repeating = 1 source, not multiple)
5. Note the earliest post date in the cluster

Respond with a JSON array of clusters:
{
  "cluster_id": "cluster-0001",
  "cluster_summary": "Multiple sources claim a temple will be announced in Boise, Idaho",
  "rumor_ids": ["rum-2025-00101", "rum-2025-00142"],
  "independent_source_count": 2,
  "earliest_post_date": "2025-06-15"
}

Claims to cluster:
